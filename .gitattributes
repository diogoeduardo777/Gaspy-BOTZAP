# Scripts do Windows precisam de fim de linha CRLF, senão o cmd/PowerShell pode
# interpretar errado (ex: "'X' não é reconhecido como um comando").
*.bat text eol=crlf
*.ps1 text eol=crlf

# Scripts de shell (Linux/Mac) sempre com LF.
*.sh text eol=lf
